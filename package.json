{
  "name": "wrdl",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "find": "ts-node src",
	"build": "tsc --pretty",
	"build:clean": "rm -rf dist/ && yarn build",
	"prepublish": "yarn build:clean"
  },
  "dependencies": {
    "axios": "^0.25.0",
    "cli-progress": "^3.10.0",
    "commander": "^9.0.0"
  },
  "devDependencies": {
    "@types/cli-progress": "^3.9.2",
    "@types/node": "^17.0.15",
    "jest": "^27.5.0",
    "ts-node": "^10.4.0",
    "typescript": "^4.5.5"
  }
}
